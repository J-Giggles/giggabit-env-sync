/**
 * @deprecated Import from `./cli-output.mjs` — re-exported for existing call sites.
 */
export {
  SYNC_LABEL,
  syncInfo,
  syncDetail,
  syncWarn,
  syncError,
  syncSuccess,
  syncDim,
  isVerbose,
  CliError,
  printCliFailure,
  printCommandHeader,
} from "./cli-output.mjs";
