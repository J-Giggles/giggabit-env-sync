/**
 * Layout helpers for `env:sync:links` — see `cli-output.mjs`.
 */
export {
  printBanner,
  printSection,
  printField,
  printTable,
  printTip,
  printNote,
  printWarning,
} from "./cli-output.mjs";
